<resources>
  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/color_primary</item>
    <item name="colorPrimaryDark">@color/color_primary_dark</item>
    <item name="colorAccent">@color/color_accent</item>
  </style>

  <!-- Style for music categories -->
  <style name="CategoryStyle">
    <item name="android:layout_width">match_parent</item>
    <item name="android:layout_height">@dimen/list_item_height</item>
    <item name="android:gravity">center_vertical</item>
    <item name="android:padding">@dimen/category_view_padding</item>
    <item name="android:layout_margin">@dimen/category_view_margin</item>
    <item name="android:textColor">@android:color/white</item>
    <item name="android:textStyle">bold</item>
    <item name="android:textAllCaps">true</item>
    <item name="android:textSize">@dimen/category_text_size</item>
  </style>

  <!-- Style for list_item.xml -->
  <style name="ListItemStyle">
    <item name="android:layout_height">wrap_content</item>
    <item name="android:layout_width">match_parent</item>
  </style>

  <!-- Style for ImageView in list_item.xml -->
  <style name="PlayIconStyle">
    <item name="android:layout_width">@dimen/play_icon_layout_width</item>
    <item name="android:layout_height">@dimen/play_icon_layout_height</item>
  </style>
</resources>
